import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
  <app-group-validator>
  </app-group-validator>
  <app-validator-enable-if>
  </app-validator-enable-if>
  `,
})
export class AppComponent {
}
